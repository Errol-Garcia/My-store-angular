import { ChangeVocalToNumberPipe } from './change-vocal-to-number.pipe';

describe('ChangeVocalToNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new ChangeVocalToNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
